<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.geometry.Pos?>

<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="player.ui.MainViewController">

    <top>
        <VBox spacing="10">
            <Label text="🎵 Music Player" styleClass="header"/>

            <HBox alignment="CENTER_LEFT" spacing="8"
                  style="-fx-background-color: white; -fx-background-radius: 20; -fx-padding: 5 10; -fx-border-color: #ccc; -fx-border-radius: 20;"
                  prefHeight="40.0">

                <ImageView fx:id="searchIcon" fitHeight="20" fitWidth="20">
                    <image>
                        <Image url="@search.png"/>
                    </image>
                </ImageView>

                <TextField fx:id="searchField"
                           promptText="Search"
                           style="-fx-background-color: transparent; -fx-border-color: transparent; -fx-font-size: 14;"
                           onKeyReleased="#handleSearch"/>
            </HBox>
        </VBox>
    </top>

    <center>
        <VBox alignment="CENTER" spacing="15">
            <Label fx:id="songTitleLabel" text="Currently playing: -" styleClass="song-title"/>

            <HBox alignment="CENTER" spacing="10">
                <Button fx:id="playButton" text="▶ Play" onAction="#handlePlay"/>
                <Button fx:id="pauseButton" text="❚❚ Pause" onAction="#handlePause"/>
                <Button fx:id="stopButton" text="■ Stop" onAction="#handleStop"/>
                <Button fx:id="nextButton" text="⏭ Next" onAction="#handleNext"/>
            </HBox>

            <Slider fx:id="progressSlider" min="0" max="100" value="0" prefWidth="300"/>
        </VBox>
    </center>

    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>
</BorderPane>